; Tests the DIV instruction
	LOAD32	R0, @2048
	LOAD32	R1, @32
	LOAD32	R2, @64
	DIV	R3, R0, R1
	CMP	R3, R2
	FAILNZ	0x01
	
	LOAD32	R1, @33
	LOAD32	R2, @62
	DIV	R3, R0, R1
	CMP	R3, R2
	FAILNZ	0x02

	LOAD32	R2, @1024
	DIV	R3, R0, 2
	CMP	R3, R2
	FAILNZ	0x03

	STOP
