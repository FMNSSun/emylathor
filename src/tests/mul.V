; Tests the MUL instruction
	LOAD32	R0, @5
	LOAD32	R1, @4
	MUL	R2, R0, R1
	FAILIC	0x01
	LOAD32	R3, @20
	CMP	R2, R3
	FAILNZ	0x02

	; MUL with immediate
	LOAD32	R0, @1024
	MUL	R0, R0, 2
	FAILIC	0x03
	LOAD32	R3, @2048
	CMP	R0, R3
	FAILNZ	0x04

	LOAD32	R0, @67108863
	LOAD32	R1, @80
	LOAD32	R2, @1073741744
	MUL	R0, R0, R1
	FAILNC	0x05
	CMP	R0, R2
	FAILNZ	0x06

	STOP
